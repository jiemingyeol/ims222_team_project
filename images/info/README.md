# Info images (Carson)

Put images used on `info.html` here.

Link them from `info.html` like this:

    <img src="images/info/your-file.jpg" alt="describe the image">

Naming: lowercase, hyphens instead of spaces, no Korean, keep the extension
lowercase (`.jpg`, not `.JPG`). Compress large photos before uploading.
