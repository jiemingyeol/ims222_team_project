# Contact images (Owen)

Put images used on `contact.html` here.

Link them from `contact.html` like this:

    <img src="images/contact/your-file.jpg" alt="describe the image">

Naming: lowercase, hyphens instead of spaces, no Korean, keep the extension
lowercase (`.jpg`, not `.JPG`). Compress large photos before uploading.
