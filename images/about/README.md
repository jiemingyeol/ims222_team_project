# About Us images (Morgan)

Put images used on `about.html` here (team photos, etc.).

Link them from `about.html` like this:

    <img src="images/about/your-file.jpg" alt="describe the image">

Naming: lowercase, hyphens instead of spaces, no Korean, keep the extension
lowercase (`.jpg`, not `.JPG`). Compress large photos before uploading.
